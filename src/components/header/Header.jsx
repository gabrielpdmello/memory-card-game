import "./Header.css"

function Header() {
    return (<>
        <header>
            <h1>Memory Card Game</h1>
            <p>Get points by clicking on cards, but don't click on the same card twice!</p>
        </header>
    </>)
}

export default Header